<rax-image
    style="{{style}}" 
    class="{{className}}"
    source="{{{uri: uri}}}"
    onClick="onClick"
    onError="onError"
    onLoad="onLoad"
    resizeMode="{{resizeMode}}"
    lazyLoad="{{lazyload}}"
/>